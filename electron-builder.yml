appId: com.interviewbuddy.app
productName: Interview Prep Assistant
directories:
  output: release
  buildResources: build
files:
  - "dist/**/*"
  - "dist-electron/**/*"
win:
  target:
    - portable
  artifactName: "${productName}-${version}.${ext}"
